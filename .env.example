# Site (JSON array of allowed origins for CORS and cookie rules)
SITE_URL=["http://localhost:3000"]

# Auth
APP_JWT_SECRET=

# AI — system keys (optional if all users use BYOK)
OPENAI_API_KEY=
GEMINI_API_KEY=

# AI — encrypt user BYOK keys in HttpOnly cookies (32+ char secret recommended; used as SHA-256 input)
AI_COOKIE_SECRET=
